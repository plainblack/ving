import { describe, test, expect } from "vitest";
import { setup, $fetch, url } from '@nuxt/test-utils'

describe('Test API', async () => {

    await setup({
        // test context options
        server: true,
    })

    test('get /api/test', async () => {
        const result = await $fetch(url('/api/test'))
        //const result = {};
        console.log(result)
        expect(result.data.success).toBe('brooks');
    });
})